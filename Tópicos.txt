- Interação com Form (GET / POST);

- Variáveis Globais ($_GET / $_POST) e isset();

- Autenticação de Usuários;

- Sessões e Proteção de Páginas Restritas;

- Incorporando Scripts (include / require);

- Encerrando a Sessão (unset() / session_destroy());

- Abertura(fopen), Escrita(fwrite) e Fechamento(fclose) de Arquivos de Texto;

